services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: arena
      POSTGRES_USER: arena
      POSTGRES_PASSWORD: localdev
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arena -d arena"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      RUST_LOG: arena_server=debug,tower_http=debug
      DATABASE_URL: postgres://arena:localdev@postgres:5432/arena
      RATE_LIMIT_BURST_SIZE: 10
      RATE_LIMIT_PER_SECOND: 5
      # MAX_AGENT_RESPONSE_ATTEMPTS: 1   # Max responses a creator agent can submit per request
      # CORS_ALLOWED_ORIGINS: https://example.com
      # ADMIN_EMAIL: you@example.com
      # PROSODY_AGENT_API_KEY: your-secret-api-key
      # MEANING_AGENT_API_KEY: your-secret-api-key
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
